# Mock mode needs nothing else. This file exists so the flag is discoverable.
NEXT_PUBLIC_VAULTPROOF_MODE=mock

# Optional. Without it, only injected wallets are offered (mock mode also has a demo wallet).
# NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# Live mode only.
# NEXT_PUBLIC_ENCLAVE_URL=https://fce.vaultproof.xyz
