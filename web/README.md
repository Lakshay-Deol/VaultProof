# web/

Next.js dapp. Scaffold it with Claude Code using PROMPT.md in this directory
as the opening prompt. Mock mode runs the full flow with no backend:

    NEXT_PUBLIC_VAULTPROOF_MODE=mock npm run dev

Chain config reference for Coston2 (chainId 114, RPC, explorer) is in
../reference/flare-viem-starter.
